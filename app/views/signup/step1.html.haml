.signup-container
  = render "signup-header"
  %main.signup-container__main
    %section.signup-container__main__container
      %h2.signup-container__main__container__title 会員情報入力
      = form_for @user, url: step2_signup_index_path, html: { class: "signup-container__main__container__form"} do |f|
        .signup-container__main__container__form__content
          = hidden_field_tag :current_step, 'step1'
          .form-part
            .form-part__title
              = f.label :nickname, {class: "form-part__title__label"} do
                ニックネーム
              %span.form-part__title__require 必須
            = f.text_field :nickname, placeholder: "例)ナオ太郎", class: "form-part__single-field"
            - if @user.errors.any?
              = @user.errors[:nickname]
          .form-part
            .form-part__title
              = f.label :email, {class: "form-part__title__label"} do
                メールアドレス
              %span.form-part__title__require 必須
            = f.email_field :email, autocomplete: "email", placeholder: "PC・携帯どちらでも可", class: "form-part__single-field"
            - if @user.errors.any?
              = @user.errors[:email]
          .form-part
            .form-part__title
              = f.label :password, {class: "form-part__title__label"} do
                パスワード
              %span.form-part__title__require 必須
            = f.password_field :password, autocomplete: "new-password", placeholder: "7文字以上", class: "form-part__single-field"
            - if @user.errors.any?
              = @user.errors[:password]
          .form-part
            .form-part__title
              = f.label :password_confirmation, {class: "form-part__title__label"} do
                パスワード（確認）
              %span.form-part__title__require 必須
            = f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "再度入力してください", class: "form-part__single-field"
            - if @user.errors.any?
              = @user.errors[:password_confirmation]
          .form-part
            .form-part__title
              %span.form-part__title__label
                本人確認
            .form-part__text
              安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
          .form-part
            .form-part__title
              = f.label :myoji_kanji, {class: "form-part__title__label"} do
                お名前（全角）
              %span.form-part__title__require 必須
            = f.text_field :myoji_kanji, placeholder: "例)福田", class: "form-part__half-field"
            = f.text_field :namae_kanji, placeholder: "例)尚", class: "form-part__half-field"
          .form-part
            .form-part__title
              = f.label :myoji_kana, {class: "form-part__title__label"} do
                お名前カナ（全角）
              %span.form-part__title__require 必須
            = f.text_field :myoji_kana, placeholder: "例)フクダ", class: "form-part__half-field"
            = f.text_field :namae_kana, placeholder: "例)ナオ", class: "form-part__half-field"
          .form-part
            .form-part__title
              = f.label :birthday, {class: "form-part__title__label"} do
                生年月日
              %span.form-part__title__require 必須
            = raw sprintf(f.date_select(:birthday, {use_month_numbers: true, start_year: 1900, date_separator: '%s'}, {class: "form-part__birthday"}), '年', '月')+'日'
            .form-part__text__info
              ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
          .actions
            = f.submit "次へ進む", class: "actions__button"
  = render "signup-footer"
