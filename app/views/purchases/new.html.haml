.wrapper 
  .header
    .header__logo
      = link_to root_path do
        = image_tag 'logos/fmarket_logo_red.svg', size: "134x40", class: "logo-image"
  .container  
    .body 
      .body__header
        購入内容の確認
      .body__main
        .body__main__top
          .body__main__top--img
            = image_tag @item.images.first, size: "60x60"
          .body__main__top--name
            #{@item.name}
        .body__main__price
          .body__main__price--amount
            ¥#{@item.price.to_s(:delimited)}
          .body__main__price--message
            送料込み
        .body__main__point
          ポイントはありません
        .body__main__payment__amount
          .body__main__payment__amount--left
            支払い金額
          .body__main__payment__amount--right
            ¥#{@item.price.to_s(:delimited)}
        .body__main__note
        .body__main__buy--btn
          = form_with url: item_purchases_path(@item.id), method: "post" do
            :plain
              <script type="text/javascript" src="https://checkout.pay.jp" class="payjp-button" data-payjp= "3b2af2d309980321d4184814ecd6257e123d9cbe" data-key="pk_test_7a80698bf3309ecfeed80515" data-text="購入する"></script>
        .body__main__info
          アプリをお持ちでない方は以下よりインストールしてご利用いただけます。
        .body__main__logo
          .body__main__logo--left
            = link_to "https://apps.apple.com/jp/app/id667861049?l=ja" do
              = image_tag 'badges/App_Store_Badge.svg', class: "logo-image"
          .body__main__logo--right
            = link_to "https://apps.apple.com/jp/app/id667861049?l=ja" do
              = image_tag 'badges/google-play-badge.png', size: "134x40", class: "logo-image"
        .address
          %ul
            %li.address__header
              配送先
            %li.address__post-number
              〒#{@user.address.postal_code}
            %li.address__main
              #{@user.address.prefecture} 
              #{@user.address.city} 
              #{@user.address.address}
              #{@user.address.building}
            %li.address__name
              #{current_user.myoji_kanji}
              #{current_user.namae_kanji}
            %li.address__footer
              = link_to root_path do
                変更する >
        .bottom
          %ul
            %li.bottom__header
              支払い方法
            %li.bottom__main
              日付
            %li.bottom__footer
              = link_to root_path do
                変更する >